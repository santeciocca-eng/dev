{
  "name": "dev-pwa",
  "version": "1.0.0",
  "private": true,
  "description": "PWA installabile e wrapper Capacitor per Android/iOS",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "@capacitor/android": "latest",
    "@capacitor/ios": "latest"
  }
}
